# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 릴케어팜 (Reel Care Farm) — 프로젝트 맥락

## 사업 개요
- 중고 낚시릴 위탁 판매/관리 서비스. 판매자는 문 앞에 릴을 두면(홈택배 수거) 릴케어팜이 검수→가격전략→판매→정산까지 전 과정을 대행.
- 무료 중고 릴 시세 조회 기능을 홈페이지 진입점으로 제공 → 방문자를 위탁 신청으로 전환시키는 게 목적.
- 홈/시세조회 화면은 상업적 문구를 배제하고 "중립적인 유틸리티 도구"처럼 보이게 유지 (즐겨찾기·재방문 유도). 프로모션 문구는 결과 페이지(view-price)에만 넣는다.

## 기술 스택 및 배포 대상
- 이 저장소는 **두 개의 별개 Apps Script 프로젝트**를 하나의 clasp 루트에 함께 보관한다 (`.clasp.json`의 `scriptId` 하나로 push되므로 실제로는 CustomerDB/웹앱 프로젝트가 메인, 시세수집 관련 스크립트는 별도 배포판일 수 있음 — 어느 파일이 어느 배포에 속하는지 확실치 않으면 작업 전 확인).
- 백엔드: Google Apps Script (V8 런타임, `appsscript.json` 참고)
  - `Code.js` — 메인 웹앱 서버 로직 (`doGet`, CustomerDB CRUD, 정산 계산, 텔레그램 알림, `onEdit` 트리거)
  - `ReelPriceCollect.js` — 외부(네이버 카페 등)에서 수집한 시세 데이터를 받는 `doPost` 웹훅 + 모델명 파싱/정규화 로직. **`Code.js`와 `doGet`/`doPost`가 분리되어 있어 별도의 웹앱 배포로 추정됨.**
  - `sortPDB.js` / `SyncToPDB.js` — 스프레드시트 커스텀 메뉴(`onOpen`)에서 수동 실행하는 시트 관리 스크립트. `시세수집` 원본 데이터를 `SpinPDB`/`BaitPDB`(시세 산정용 내부 DB)로 반영하고 정렬함. 웹앱과 무관하게 스프레드시트 UI에서 직접 실행.
- 프론트: 단일 HTML SPA (`Index.html`, `doGet`이 서빙) — `view-home` / `view-price` / `view-apply` / `view-check` / `view-success` / `view-address` 등 `.page` div를 `showPage(id)`로 전환하는 방식. 서버 호출은 전부 `google.script.run`.
- 데이터: Google Sheets — 시트별 컬럼 구조는 [SheetStructure.md](SheetStructure.md) 참고 (자동 추출본, 실제 시트가 최신 진실 소스).
  - `CustomerDB` — 고객/위탁 거래 전체 라이프사이클. 컬럼 순서는 `Code.js`의 `COL`/`IDX` 상수로 관리 (1-based/0-based 동시 제공).
  - `베이트시세표` / `스피닝시세표` — 정가 기준 시세 조회에 쓰이는 참조표 (`getManufacturers`, `getModelsAndDetails`, `getPriceRange`).
  - `시세수집` — 외부 크롤링/제보로 들어온 원본 시세 데이터 (`doPost` in ReelPriceCollect.js가 적재).
  - `SpinPDB` / `BaitPDB` — `시세수집`을 가공해 만든 내부 통계 DB (최저/평균/최고가). `syncToPDB()`가 채우고 `sortPDB()`가 정렬.
  - `검수DB` — 상품별 검수 항목/점수를 열(column) 단위로 저장하는 특수 구조 (행=항목, 열=검수번호별 상품). `getInspectionData()`가 파싱.
  - `DB컬럼설명` — `현재상태`별 기본 안내 코멘트 매핑 (`getDefaultCommentForStatus()`가 조회).
- GitHub 저장소: gameter/ReelCarepam

## 정산 로직 (중요 — 항상 이 공식 기준으로 계산)
- 수수료: 위탁자 판매 희망가(릴케어 인증 판매는 인증료 2만원 제외한 순가격) 기준 17%
- 최저 수수료: 12만원 이하 상품은 수수료 최저 2만원 적용
- 정산금액 = 판매 희망가 − 수수료 − 배송료(기본 4,000원) − (필요 시 정비비/부품비)
- 릴케어 인증 판매는 순가격에 인증료 2만원을 더해 최종 판매가를 구성 (계산은 `calculateFinancials()` 참고, `Code.js:462`)
- 홈택배 배송비는 위탁 시점에 결제되지 않고 정산 시 자동 차감됨 (절대 "무료 방문수거"라고 표현하지 말 것 — 오해 소지 있는 문구는 이미 제거된 상태)

## 상태(status) 워크플로우
`CustomerDB`의 `현재상태` 컬럼은 다음 값들을 순환하며, 일부 전이는 `onEdit()` 트리거가 검증/가드한다:
`픽업/입고대기` → `입고/검수대기` → `전략협의` → (`베이직 판매` | `릴케어 인증 판매`) → `판매중` → (`판매정지` | `거래완료`) / `계좌정보제공` → `송금대기` → `거래완료` / `진행보류`
- `전략협의`로 바꾸려면 검수번호 + 빠른/적정/높은 매도가 3종이 모두 채워져 있어야 함 — 없으면 alert 후 `입고/검수대기`로 되돌림.
- `판매중`으로 바꾸려면 판매링크(K열)가 있어야 함 — 없으면 alert 후 `베이직 판매`로 되돌림.
- `거래완료`가 되면 정산 재계산 + 판매완료일 기록.
- 상태 변경 시마다 `DB컬럼설명` 시트에서 기본 코멘트를 자동으로 채워 넣는다.
- 주요 전이 지점(신청 접수, 전략 확정, 계좌 입력)마다 `sendTelegramNotify()`로 텔레그램 알림 발송.

## GAS 배포 관련 — 반드시 지킬 것
- **GitHub에 push/sync 해도 라이브 `/exec` URL은 자동으로 안 바뀜.** 새 버전을 쓰려면 Apps Script 편집기에서 "배포 관리 → 새 배포"를 수동으로 눌러야 프로덕션에 반영됨.
- **테스트 배포(`/dev` URL)는 저장 즉시 반영됨** — 브라우저에서 F5만 새로고침하면 됨.
- **작업 시 기본 가정: 별다른 말 없으면 항상 테스트 배포(`/dev`) 기준으로 작업 중이라고 간주.** 새 배포(프로덕션 반영)는 명시적으로 요청받았을 때만 언급.
- Apps Script 편집기 탭을 새로고침 안 하고 열어둔 채로 편집하면 서버의 최신본을 덮어쓸 위험 있음 — 편집 전 항상 최신 상태 확인.
- 로컬에서 clasp로 push하려면 `clasp push` (`.clasp.json`의 scriptId 대상). 이 저장소에는 자동화된 빌드/린트/테스트 스크립트가 없음 — `package.json`은 타입 정의(`@types/google-apps-script`)만 의존성으로 가짐. 수정 후 검증은 Apps Script 편집기/브라우저에서 수동으로 진행.

## CSS/프론트 관련 함정
- JS로 주입한 인라인 `style="..."`는 항상 클래스 기반 CSS(미디어쿼리 포함)보다 우선순위가 높음 → 오버라이드하려면 스타일시트에 `!important` 필요.
- GAS iframe 내부에서는 `window.innerWidth`, `window.matchMedia()`가 고정값(~980px)을 반환해 모바일 감지에 못 씀 → `navigator.userAgent`로 모바일 판별할 것 (Index.html의 `searchAddress()` 함수 참고).

## 데이터/콘텐츠 원칙
- **가짜/추정 통계, 플레이스홀더 수치를 절대 UI에 넣지 않는다.** (예: 위탁 건수, 최근 가입자 수 등은 실제 운영 데이터 없으면 표시 안 함)
- 고객에게 오해를 줄 수 있는 문구(허위 광고성 표현)는 쓰지 않는다.

## 코드 스타일 / 작업 방식
- 기존 함수/변수명, 구조를 최대한 유지하고 최소 diff로 수정.
- 전체 파일을 통으로 다시 쓰지 말고, 변경된 함수/블록만 명확히 제시.
- 기능은 단계적으로 만들고 테스트: 데이터/인프라가 아직 없는 기능은 무리해서 완성하지 않고 보류.

## 현재 진행 중 / 보류된 작업 (참고용, 최신 상태는 병철님께 확인)
- 위탁 상품 UI 패널 폰트/문구 정리
- 텔레그램 봇 양방향 명령 (/조회, /미결제 등)
- 카카오 알림톡 고객 발송 (사업자등록 필요, 아직 미완료)
- 네이버 스마트스토어 자동 등록 파이프라인
- 초기 재고 확보 (개인 릴 시딩, 당근마켓/중고나라 아웃바운드)
- 카카오채널 URL 플레이스홀더(`여기에_카카오채널_URL`) 교체 — 실제 채널 링크 확정되면

## 병철 추가 내용
 - 이 프로젝트 작업 시 항상 한국어로 응답할 것
 - clasp push 이나 GitHub push는 내가 수동으로 할게 묻지마.. 넌 수정 까지만 해줘